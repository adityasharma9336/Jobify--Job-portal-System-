apiVersion: v1
kind: ConfigMap
metadata:
  name: jobify-config
  namespace: jobify
  labels:
    app.kubernetes.io/name: jobify
data:
  # Non-sensitive configuration
  NODE_ENV: "production"
  PORT: "5000"
  MONGO_HOST: "mongo-service"
  MONGO_PORT: "27017"
  MONGO_DB: "jobify"
