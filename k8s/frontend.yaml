apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobify-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jobify-frontend
  template:
    metadata:
      labels:
        app: jobify-frontend
    spec:
      containers:
      - name: frontend
        image: your-dockerhub-username/jobify-frontend:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: jobify-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
